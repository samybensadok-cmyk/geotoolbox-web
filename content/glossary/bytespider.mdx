---
term: "Bytespider"
aliases: ["ByteDance crawler"]
definition: "Bytespider is ByteDance's web crawler, used to collect data to train its large language models. It is known for crawling aggressively and reportedly does not always respect robots.txt, so blocking it often takes a server or WAF rule rather than robots.txt alone."
category: "AI Crawlers"
related: ["gptbot", "ai-crawler", "robots-txt"]
article: "/blog/how-to-optimize-for-ai-search"
articleLabel: "How to Optimize for AI Search"
updated: "2026-05-31"
draft: false
---

Bytespider identifies with the user agent `Bytespider`. Unlike a search crawler, its purpose is gathering training data for ByteDance's AI models, so allowing it offers little visibility benefit in Western AI search.

Because it reportedly ignores `robots.txt` in some cases, owners who want to block it usually do so at the server or firewall level by matching the user agent, not in `robots.txt` alone.
